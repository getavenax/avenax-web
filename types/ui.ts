/**
 * Shared UI prop contracts (Layer A public vocabulary).
 * `types/` may be imported from any layer and imports nothing from components.
 */

/** Form-control sizing — md is the 44px touch-target default. */
export type ControlSize = "sm" | "md";
